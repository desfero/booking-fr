/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TimesPipe } from './times.pipe';

describe('TimesPipe', () => {
  it('create an instance', () => {
    let pipe = new TimesPipe();
    expect(pipe).toBeTruthy();
  });
});
